<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="#FFF"
    android:orientation="vertical" >
    
    <ImageView
        android:id="@+id/compose_picture"
        android:layout_height="50dip"
        android:layout_width="50dip"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:padding="5dp"
        android:background="#333"
        android:contentDescription="Picture for story"
        android:src="@drawable/ic_action_new_picture" />

    <EditText
        android:id="@+id/compose_title"
        android:layout_width="match_parent"
        android:layout_height="50dip"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@id/compose_picture"
        android:background="#DDD"
        android:paddingLeft="10dp"
        android:hint="Title"
        android:singleLine="true" >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/compose_story"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/compose_picture"
        android:gravity="top|left"
        android:hint="Write your story."
        android:inputType="textMultiLine"
        android:lines="8"
        android:minLines="6"
        android:scrollbars="vertical" />


</RelativeLayout>